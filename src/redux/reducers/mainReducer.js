const initialState = {
    headValues: ['home', 'about', 'team', 'booking', 'menu', 'galerie', 'events', 'contact'],
}

export const mainReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}